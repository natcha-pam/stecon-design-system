namespace Stecon.UI;

/// <summary>Single-column sort direction for <see cref="SteconDataTable{TItem}"/>. Multi-column sort is deferred.</summary>
public enum SortDirection
{
    None,
    Ascending,
    Descending
}
