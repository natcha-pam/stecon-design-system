namespace Stecon.UI;

/// <summary>Table cell text wrapping preference. NoWrap never disables the table wrapper's own horizontal scrolling.</summary>
public enum SteconTableWrapMode
{
    Wrap,
    NoWrap,
}
