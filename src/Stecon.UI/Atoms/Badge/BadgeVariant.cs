namespace Stecon.UI;

/// <summary>Semantic badge coloring. Maps to Foundation status tokens, never brand colors directly.</summary>
public enum BadgeVariant
{
    Neutral,
    Info,
    Success,
    Warning,
    Danger
}
