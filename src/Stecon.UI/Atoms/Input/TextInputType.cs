namespace Stecon.UI;

/// <summary>Closed set of native &lt;input type&gt; values supported by <see cref="SteconInput"/> in v1.</summary>
public enum TextInputType
{
    Text,
    Email,
    Password,
    Number,
    Search,
    Tel
}
